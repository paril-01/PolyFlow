apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service-python
  namespace: ecp
  labels:
    app: ai-service-python
    team: ml
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ai-service-python
  template:
    metadata:
      labels:
        app: ai-service-python
    spec:
      containers:
      - name: ai-service-python
        image: ecp/ai-service-python:latest
        ports:
        - containerPort: 8089
        env:
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8089
          initialDelaySeconds: 30
          periodSeconds: 10
