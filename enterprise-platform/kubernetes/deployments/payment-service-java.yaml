apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service-java
  namespace: ecp
  labels:
    app: payment-service-java
    team: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: payment-service-java
  template:
    metadata:
      labels:
        app: payment-service-java
    spec:
      containers:
      - name: payment-service-java
        image: ecp/payment-service-java:latest
        ports:
        - containerPort: 8082
        env:
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
