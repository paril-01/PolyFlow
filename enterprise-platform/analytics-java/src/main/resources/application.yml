server:
  port: 8088
spring:
  application:
    name: analytics-java
  datasource:
    url: jdbc:postgresql://localhost:5432/analytics_java
    username: ecp
    password: ecp_secret
  jpa:
    hibernate:
      ddl-auto: validate
logging:
  level:
    root: INFO
    com.ecp: DEBUG
