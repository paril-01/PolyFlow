# User Create — PolyFlow Governance Module
# Service: frontend-angular | Language: typescript | Domain: admin

@contract
feature_id: "ECP-ADMIN-USER-CREATE"
owner: "Iris Nakamura <iris.nakamura@ecp.dev>"
team: "platform"
classification: "sensitive"
approvers: ["iris.nakamura@ecp.dev", "hassan.ali@ecp.dev"]
retention_years: 7
timeout_ms: 3000
@end

@schema UserCreateRequest
  id: string<format:uuid>
  timestamp: string<format:datetime>
  metadata: object | null
@end

@schema UserCreateResponse
  id: string<format:uuid>
  status: string
  processing_time_ms: number
  result: object | null
  errors: array<string>
@end

@standard language="typescript"
allowed_imports: ["react", "axios", "@tanstack/react-query"]
@end

@typescript[service]
import React, { useState } from 'react';

interface UserCreateProps {
  onComplete?: (data: any) => void;
}

export const UserCreate: React.FC<UserCreateProps> = ({ onComplete }) => {
  const [loading, setLoading] = useState(false);

  const handleClick = async () => {
    setLoading(true);
    try {
      const res = await fetch('/api/v1/admin/user-create', { method: 'POST' });
      const data = await res.json();
      onComplete?.(data);
    } finally {
      setLoading(false);
    }
  };

  return (
    <button onClick={handleClick} disabled={loading}>
      {loading ? 'Processing...' : 'User Create'}
    </button>
  );
};
@end

@merge strategy="all-success" order=["typescript"]
@end

@error-map language="typescript"
  "TypeError" -> "Type mismatch in user_create component."
  "NetworkError" -> "API call failed for user_create."
@end

@rationale for="user_create"
reason: "Selected typescript for User Create due to ecosystem maturity and team expertise."
alternatives_considered: ["rust", "elixir"]
rejected_reasons: {"rust": "Team lacks Rust experience", "elixir": "Insufficient library support"}
@end

@decision for="user_create"
decision: "Implement as microservice endpoint on frontend-angular"
date: "2024-06-15"
status: "accepted"
stakeholders: ["Iris Nakamura"]
@end

@audit
action: "Log User Create execution"
fields: ["status", "trace_id", "processing_time_ms"]
retention: "7_years"
compliance: ["SOC2", "PCI-DSS"]
@end
